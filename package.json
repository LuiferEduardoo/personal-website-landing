{
  "name": "personal-website-landing",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "NODE_OPTIONS='--dns-result-order=ipv4first' astro dev",
    "build": "NODE_OPTIONS='--dns-result-order=ipv4first' astro build",
    "preview": "astro preview",
    "astro": "astro",
    "preview:cf": "astro build && npx wrangler dev -c wrangler.deploy.toml",
    "deploy": "astro build && npx wrangler deploy -c wrangler.deploy.toml"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.2.1",
    "astro": "^6.1.7"
  }
}
